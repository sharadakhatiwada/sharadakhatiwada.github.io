const numbers = [2, 3, 4, 5];
const numberArray = numbers.map(double);

function double(item) {
  const newArr = [];
  for (let num of arr) {
    num = num * 2;
    newArr.push(num);
  }
  return newArr;
}
double(numbers);
